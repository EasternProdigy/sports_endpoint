{
  "name": "matrix-scoreboard-worker",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker control+score API for MatrixPortal scoreboard",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.65.0"
  }
}
